pub fn main Void world World !
  mut out [6]u8 [0_u8;6]
  let actual std.str.reverse out "drawer"
  if actual.has
    if std.mem.eql actual.value "reward"
      check world.out.write "reverse string ok\n"
